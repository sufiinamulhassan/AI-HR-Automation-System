# Local development and CI only — never installed by the Vercel build.
# The deployable set is requirements.txt; this adds what is needed to run
# the test suite against it.
-r requirements.txt

pytest>=9.0.0
pytest-asyncio>=0.23.0
